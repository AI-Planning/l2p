You are a PDDL domain expert. I want you to generate a complete PDDL domain.

Output the complete, valid PDDL domain inside a fenced code block labeled `pddl`:

```pddl
(define (domain <name>)
    (:requirements <...>)
    (:types <...>)      ; OPTIONAL
    (:constants <...>)  ; OPTIONAL
    (:predicates <...>)
    (:functions <...>)  ; OPTIONAL
    (:action <...> ...)
)
```

Please process the following domain:

<domain_description>
{domain_desc}
</domain_description>

{domain_section}
