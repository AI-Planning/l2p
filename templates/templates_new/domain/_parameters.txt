Based off of the natural language description, your role is to model PDDL parameters in the following format.

End your final answer by wrapping the PDDL components inside specific XML tag `<parameters> ... </parameters>` as shown below. Do not include Markdown backticks.

<parameters>
[
  {
    "variable": "?t1",
    "type": "type_1",
    "desc": "string (optional)"
  },
  {
    "variable": "?t2",
    "type": "type_2",
    "desc": "string (optional)"
  }
]
</parameters>

If there are no parameters to be created, leave the array completely empty `[]`

Please process the following domain:
<domain_description>
{domain_desc}
</domain_description>

