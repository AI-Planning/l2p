Based off of the natural language description, your role is to model a PDDL domain in the following format.

End your final answer by wrapping the PDDL components inside specific XML tag `<domain> ... </domain>` as shown below. Do not include Markdown backticks.

<domain>
(define (domain <domain_name>)
  (:requirements :typing)
  
  (:types 
    <type_name> - <parent_type>
    <type_name> - <parent_type>
  )

  (:predicates 
    (<predicate_name> ?<param_name> - <type_name>)
    (<predicate_name> ?<param_name> - <type_name>)
  )

  (:action <action_name>
    :parameters (?<param_name> - <type_name>)
    :precondition (and 
      (<predicate_name> ?<param_name>)
    )
    :effect (and 
      (<predicate_name> ?<param_name>)
      (not (<predicate_name> ?<param_name>))
    )
  )
)
</domain>

Please process the following domain:
<domain_description>
{domain_desc}
</domain_description>