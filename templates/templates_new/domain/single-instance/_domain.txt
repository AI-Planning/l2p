Based off of the natural language description, your role is to model a PDDL domain in the following format.

End your final answer by wrapping the PDDL components inside specific XML tag `<domain> ... </domain>` with the specified JSON object as shown below. Do not include Markdown backticks.

<domain>
(define (domain logistics_domain)
  (:requirements :strips :typing)
  
  (:types 
    vehicle - object
    truck - vehicle
    location - object
  )

  (:predicates 
    (at ?v - vehicle ?l - location)
    (connected ?l1 - location ?l2 - location)
  )

  (:action drive
    :parameters (?t - truck ?from - location ?to - location)
    :precondition (and 
      (at ?t ?from)
      (connected ?from ?to)
    )
    :effect (and 
      (at ?t ?to)
      (not (at ?t ?from))
    )
  )
)
</domain>

## RULES
1. The PDDL block above is strictly an ILLUSTRATIVE EXAMPLE. Do not copy names like "logistics_domain", "truck", or "drive" 
  unless they are explicitly defined in the domain description. You must extract the actual name, types, predicates, and actions from the text.
2. Provide ONLY valid raw PDDL syntax wrapped in `<domain>` tags. Do NOT wrap the output in JSON or Markdown code blocks (like ```lisp or ```pddl).
3. The domain must start with `(define (domain domain_name))`.
4. Include all necessary `:requirements` based on the features you use (e.g., `:typing`, `:numeric-fluents`, `:durative-actions`).
5. Ensure all variables in `:parameters`, `:predicates`, `:precondition`, and `:effect` blocks are correctly prefixed with a question mark (e.g., `?t`).
6. All elements in `:types` and `:parameters` must follow the format `?variable - type`. If no parent type is specified, use `object`.
7. Ensure all LISP parentheses `()` are perfectly balanced and closed. 
8. Use standard formatting and indentation to keep the PDDL readable.

Please process the following domain:
<domain_description>
{domain_desc}
</domain_description>