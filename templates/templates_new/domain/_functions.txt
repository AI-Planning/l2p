Based off of the natural language description, your role is to model PDDL functions in the following format.

End your final answer by wrapping the PDDL components inside specific XML tag `<functions> ... </functions>` as shown below. Do not include Markdown backticks.

<functions>
[
  {
    "name": "function-name",
    "params": [
      {
        "variable": "?t1",
        "type": "type_1",
      },
      {
        "variable": "?t2",
        "type": "type_2"
      }
    ],
    "desc": "string (optional)"
  }
]
</functions>

If there are no new functions to be created, leave the array completely empty `[]`

Please process the following domain:
<domain_description>
{domain_desc}
</domain_description>