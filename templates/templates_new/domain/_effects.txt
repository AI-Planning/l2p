Based off of the natural language description, your role is to model PDDL action effects in the following format.

End your final answer by wrapping the PDDL components inside specific XML tag `<effects> ... </effects>` as shown below. Do not include Markdown backticks.

<effects>
{
  "add": [
    "(at ?r ?to)"
  ],
  "delete": [
    "(at ?r ?from)"
  ],
  "numeric": [
    "(decrease (battery-level ?r) 5)",
    "(increase (distance-travelled) 1)"
  ],
  "conditional": [
    {
      "condition": [
        "(has-payload ?r)"
      ],
      "effect": {
        "add": ["(payload-delivered ?r)"],
        "delete": [],
        "numeric": []
      },
      "desc": "If the rover has a payload, mark it as delivered upon arrival."
    }
  ],
  "desc": "Rover moves from one location to another, consuming battery."
}
</effects>

If there are no effects (or its components) to be created, leave the arrays completely empty:
{
  "add": [],
  "delete": [],
  "numeric": [],
  "conditional": [],
  "desc": null
}

Each item inside "add", "delete", "numeric", and the "condition" arrays must be a valid LogicalCondition. 

A LogicalCondition may be:
    1. A simple PDDL string:
    - "(at ?r ?l)"
    - "(decrease (battery-level ?r) 5)"

    2. A quantified dictionary (Useful for universal effects):
    - FORALL
    {
        "quantifier": "forall",
        "parameters": [
        {
            "variable": "?w",
            "type": "waypoint"
        }
        ],
        "conditions": [
        "(visited ?w)"
        ]
    }

Rules:
- "add", "delete", "numeric", and "conditional" must always be JSON arrays.
- Do NOT use the `not` operator for deletions. If an effect removes a state, simply put the plain predicate string inside the "delete" array. 
- Do NOT use the `and` operator to combine multiple effects. Simply add each effect as a separate item in the respective array.
- "numeric" should contain assignments or continuous operations (e.g. `increase`, `decrease`, `assign`).
- The "conditional" array represents the PDDL `when` operator (e.g., `(when (condition) (effect))`). Use this explicitly for effects that only trigger if a secondary condition evaluates to true at the time of execution.
- Inside "conditional", the "effect" key must be an object strictly containing "add", "delete", and "numeric" arrays.
- Keep parameter names typed consistently with the domain.

Please process the following domain:
<domain_description>
{domain_desc}
</domain_description>